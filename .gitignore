# Secrets
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Runtime data & artifacts
# keep data/topic_history.json (the dedup history — GitHub Actions needs it),
# ignore everything else under data/
data/*
!data/topic_history.json
output/
logs/
# downloaded image / trends cache — regenerated on every run
assets/cache/
*.sqlite3
*.db

# OAuth tokens
secrets/
token_*.json
client_secret*.json
